a = Math;  //will print all the math functions
b = Math.PI;
c = Math.E;
d = Math.ceil(5.7);  //it will roundoff the values to upper number
e = Math.round(5.4); //it also rounds off
f = Math.floor(5.7); //rounds off to smaller value
g = Math.abs(-4); // gives the absolute value
h = Math.sqrt(4); //gives the sqr root value 
i = Math.random(); //generatres a random number , we can also specify the range in which we want random number 
console.log(i);

